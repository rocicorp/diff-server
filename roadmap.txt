mvp
===
x generic c api which is basically a reflection of the cli (1 wk?)
- hello world that embeds api
  x android
  - ios
    - did it once but removed when futzing with api - redo
    - do it again using swiftui
    - maybe use a todo sample as a starting point?
x tx reg and execution apis (1-2 wk)
  x run js code (v8? otto?)
  x expose api via js bindings
  x js library to make it minimally easy to use
- notif api (2 days)
- sync (1 wk)
  x local-side sync
  - server-side sync
- server -> store data where? (several weeks)
  - ideally non-hostproof hosting
- blocking notifs

later
=====
- fix noms sync with long commit chains
- gc
  - collapse commit chains beyond some window
  - gc unreachable slabs in nbs
- indexes
- queries
